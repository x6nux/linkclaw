# 生产环境变量模板（复制为 .env 并填写）

# 数据库
POSTGRES_PASSWORD=your-strong-password-here

# Redis（可留空则不设密码）
REDIS_PASSWORD=

# JWT（至少 32 字符的随机字符串）
JWT_SECRET=your-jwt-secret-here-min-32-chars

# 域名（用于 CORS 和 WebSocket）
DOMAIN=yourdomain.com
